setup:
  stage: True # False takes in all cells
  cell_type: 3
  version: 'resnet18_arcface-t3'
  run_test: True 

submission:
  pattern: 'cosface'

data:
  train: 'U2OS_train_small.csv' # 'U2OS_train_small.csv'

model:
  image_size: 112
  num_classes: 1108
  arch: 'resnet18'
  
train:
  batch_size: 48
  num_epochs: 1
  
val:
  batch_size: 96

test:
  batch_size: 96
  
loss:
  name: 'normsoftmax' # arcface, cosface, adacos, sphereface, amsoftmax, focal, ls_cross_entropy, cross_entropy, normsoftmax
  params:
    in_features: 512
  
optimizer:
  name: 'adamw'
  params:
    lr: 0.0003

scheduler:
  name: 'cosine'
  params:
    eta_min: 0.00003
    T_max: 276